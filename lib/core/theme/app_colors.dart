import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white;

  static const black = Colors.black;
  static const black40 = Color.fromRGBO(0, 0, 0, 0.4);
  static const black50 = Color.fromRGBO(0, 0, 0, 0.5);
  static const black60 = Color.fromRGBO(0, 0, 0, 0.6);
  static const black70 = Color.fromRGBO(0, 0, 0, 0.7);

  static const darkGrey = Color(0xFF333333);
  static const grey = Color(0xFF828282);
  static const lightGrey = Color(0xFF9797AA);

  static const darkBlue = Color(0xFF181C37);
}
